{
  "name": "hyperinsure-tests",
  "version": "1.0.0",
  "description": "Run unit tests on this project.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:report": "vitest run -- --coverage --costs",
    "test:watch": "chokidar \"tests/**/*.ts\" \"contracts/**/*.clar\" -c \"npm run test:report\"",
    "fuzz:governance:test": "npx rv . governance test --runs 100",
    "fuzz:governance:invariant": "npx rv . governance invariant --runs 100",
    "fuzz:oracle:test": "npx rv . oracle test --runs 100",
    "fuzz:oracle:invariant": "npx rv . oracle invariant --runs 100",
    "fuzz:core:test": "npx rv . hyperinsure-core test --runs 100",
    "fuzz:core:invariant": "npx rv . hyperinsure-core invariant --runs 100",
    "fuzz:all:test": "npm run fuzz:governance:test && npm run fuzz:oracle:test && npm run fuzz:core:test",
    "fuzz:all:invariant": "npm run fuzz:governance:invariant && npm run fuzz:oracle:invariant && npm run fuzz:core:invariant",
    "fuzz:all": "npm run fuzz:all:test && npm run fuzz:all:invariant",
    "deploy:testnet": "node scripts/deploy.js testnet",
    "deploy:testnet:dry-run": "node scripts/deploy.js testnet --dry-run",
    "deploy:testnet:core": "node scripts/deploy.js testnet --core-only",
    "deploy:testnet:shell": "scripts/testnet-deploy.bat",
    "deploy:mainnet": "node scripts/deploy.js mainnet --i-understand-this-is-mainnet",
    "deploy:mainnet:dry-run": "node scripts/deploy.js mainnet --dry-run",
    "deploy:mainnet:core": "node scripts/deploy.js mainnet --core-only --i-understand-this-is-mainnet",
    "deploy:mainnet:shell": "scripts/mainnet-deploy.bat --i-understand-this-is-mainnet",
    "verify:testnet": "node scripts/verify-deployment.js testnet",
    "verify:mainnet": "node scripts/verify-deployment.js mainnet",
    "wallet:create": "node scripts/create-wallet.js",
    "wallet:funding-info": "node scripts/create-wallet.js --funding-info",
    "deployment:status": "node scripts/deployment-status.js",
    "deploy:help": "echo Available deployment commands: && echo   npm run wallet:create           - Create testnet wallet && echo   npm run wallet:funding-info     - Show funding instructions && echo   npm run deploy:testnet         - Deploy to testnet && echo   npm run deploy:testnet:dry-run - Simulate testnet deployment && echo   npm run deploy:mainnet         - Deploy to mainnet && echo   npm run verify:testnet         - Verify testnet deployment && echo   npm run verify:mainnet         - Verify mainnet deployment && echo   npm run deployment:status      - Show deployment status"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hirosystems/clarinet-sdk": "^3.0.2",
    "@mempool/mempool.js": "^3.0.0",
    "@stacks/transactions": "^7.0.6",
    "bitcoinjs-lib": "^7.0.0",
    "chokidar-cli": "^3.0.0",
    "fast-check": "^3.15.0",
    "vitest": "^3.1.3",
    "vitest-environment-clarinet": "^2.3.0"
  },
  "devDependencies": {
    "@stacks/rendezvous": "^0.12.0"
  }
}
